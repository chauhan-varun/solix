name: Deploy Solix

on:
  push:
    branches:
      - dev

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install Dependencies
        run: pnpm install
        
      - name: Generate Prisma Client
        run: pnpm prisma generate

      - name: Lint
        run: pnpm run lint
        
      - name: Build
        run: pnpm run build
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
          NEXT_PUBLIC_APP_URL: http://141.148.214.59:3000

  deploy:
    needs: lint-and-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/solix
            git pull origin dev
            
            # 1. Update Infrastructure (MQTT/Mongo)
            docker compose up -d mosquitto mongodb mqtt-bridge
            
            # 2. Update Website (using PM2)
            export DATABASE_URL="${{ secrets.DATABASE_URL }}"
            pnpm install
            pnpm prisma generate
            pnpm build
            pm2 restart solix || pm2 start "pnpm start" --name "solix"
